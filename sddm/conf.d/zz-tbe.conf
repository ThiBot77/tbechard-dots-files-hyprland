# Named zz- so it sorts after /usr/lib/sddm/sddm.conf.d/default.conf,
# which sets an empty Current= that would otherwise win.
# Left on the X11 greeter backend: the Wayland one needs a compositor.
[Theme]
Current=tbe

[Users]
# Written by SDDM itself, so empty until the first login through it.
RememberLastUser=true
RememberLastSession=true
