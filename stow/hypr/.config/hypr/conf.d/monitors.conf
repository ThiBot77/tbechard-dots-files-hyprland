# Auto-detect, preferred resolution/refresh, top-left position, scale 1.
# Run `hyprctl monitors` once under Hyprland and replace this with an
# explicit line per monitor if you need a custom layout, e.g.:
#   monitor = DP-1, 2560x1440@144, 0x0, 1
monitor = , preferred, auto, 1
